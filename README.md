# 🧠 Codebase Genius

Codebase Genius is an intelligent documentation generator that analyzes any public GitHub repository, builds a Code Context Graph (CCG), and produces human-readable markdown documentation with diagrams.

---

## 🚀 Features
- Automatically clones any public GitHub repo
- Scans the file structure and README
- Builds a Code Context Graph (functions & classes)
- Generates a visual diagram (Graphviz)
- Produces ready-to-use Markdown documentation

---

## 🛠 Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
